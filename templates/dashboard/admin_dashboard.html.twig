{% extends 'base.html.twig' %}

{% block title %}Tableau de Bord Admin{% endblock %}

{% block body %}
    <div class="container">
        <h1>Bienvenue, Admin {{ app.user.firstName }} {{ app.user.lastName }} 👨‍💻</h1>
        <p>Gestion des utilisateurs et des fichiers</p>

        <a href="{{ path('admin_users') }}" class="btn btn-primary">Gérer les utilisateurs</a>
        <a href="{{ path('admin_files') }}" class="btn btn-secondary">Voir tous les fichiers</a>
        {#<a href="{{ path('app_logout') }}" class="btn btn-danger">Déconnexion</a>#}

        <h2>📊 Statistiques</h2>
        <p>👥 Nombre d'utilisateurs : <strong>{{ userCount }}</strong></p>
        <p>📂 Nombre de fichiers uploadés : <strong>{{ fileCount }}</strong></p>
    </div>

<div class="container d-flex flex-column ">

        {# {% for item in Stores %}
            <p>{{item.name}}</p>
            <div class="progress w-25">
                <div class="progress-bar bg-primary" role="progressbar" style="width: {{item.sizeUse}}%" aria-valuenow="75" aria-valuemin="0" aria-valuemax="100"></div>
            </div>

           
        {% endfor %} #}
    </div>





</div>



<pre>
{{ dump(Stores) }}

</pre>

{% endblock %}
